#include "fabrica/world/chunk.h"
#include <fabrica/renderer/voxel_renderer.h>

void render_chunk(const fabrica_Chunk *chunk) {
    for (int x = 0; x < CHUNK_SIZE; x++) {
        for (int y = 0; y < CHUNK_SIZE; y++) {
            for (int z = 0; z < CHUNK_SIZE; z++) {

            }
        }
    }
}
